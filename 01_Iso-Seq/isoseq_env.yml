name: isoseq_env  # Replace 'my_environment' with your desired environment name
channels:
  - defaults
  - conda-forge  # Use the conda-forge channel for some packages if needed
dependencies:
  - bamtools
  # Assuming pbindex is provided by PacBio's software suite, which might have different installation methods
  # You might need to check PacBio's documentation or specific instructions on how to install pbindex
