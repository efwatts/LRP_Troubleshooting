# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: refined-database-generation
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Module 8: Refined Db Generation
  - python=3.7.3
  - biopython=1.79
  - numpy=1.21.6
  - pandas=1.3.5
